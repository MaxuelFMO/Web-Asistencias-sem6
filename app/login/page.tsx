import LoginView from '@/views/LoginView';

export const metadata = {
  title: 'Login - Control de Visitas',
  description: 'Inicia sesión en el sistema de control de visitas',
};

export default function LoginPage() {
  return <LoginView />;
}
