{
  "name": "web-asistencias",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix front",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix front\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}